# PYGMIX お問い合わせフォーム設計

## 現在の状態

この文書は設計のみ。フォーム実装、外部サービス登録、DNS変更、Sitesの環境変数設定、公開はまだ行わない。

- 公開サイト: `https://pygmix.com/`
- 想定受信先: `info@pygmix.com`
- 2026-08-31時点で `pygmix.com` にはCloudflare Email RoutingのMX/SPFレコードがある。
- `info@pygmix.com` 個別の転送設定と実受信は未確認。公開前に実メールで確認する。
- 現在のSites設定はD1/R2なし。問い合わせ内容はデータベースへ保存しない。

## 採用する構成

### 画面

- 新規ページ: `/contact`
- トップの `CONTACT` は `mailto:` ではなく `/contact` へ移動する。
- System Audio Analyzerの問い合わせ導線も、将来 `/contact?category=tool&subject=System%20Audio%20Analyzer` に統一する。
- 送信完了後は同じページ内に完了表示を出す。二重送信を避けるため、送信中はボタンを無効化する。
- JavaScriptが動かない場合は送信できない旨と再読み込み案内を表示する。

### 入力項目

| 項目 | 必須 | 制限・初期値 |
| --- | --- | --- |
| お名前 | 必須 | 1〜100文字 |
| メールアドレス | 必須 | 254文字以内、形式確認 |
| ご所属 | 任意 | 120文字以内 |
| お問い合わせ種別 | 必須 | 制作依頼 / 上演・出演 / 取材 / ツール・アプリ / その他 |
| 件名 | 任意 | 150文字以内。URLの `subject` から初期値を設定可能 |
| お問い合わせ内容 | 必須 | 10〜5000文字 |
| 個人情報の取扱いへの同意 | 必須 | 未選択では送信不可 |

添付ファイル、電話番号、住所は初期版では扱わない。必要以上の個人情報を集めず、運用と迷惑送信対策を簡潔に保つ。

### 送信処理

- サーバー側エンドポイント: `POST /api/contact`
- 推奨メール送信サービス: ResendのHTTP API
- 宛先: `info@pygmix.com`
- 送信元: `PYGMIX Website <contact@send.pygmix.com>`
- `Reply-To`: フォームに入力されたメールアドレス
- 件名: `[PYGMIX CONTACT] {お問い合わせ種別} / {お名前}`
- 本文: 受付日時、種別、氏名、所属、メールアドレス、件名、本文、受付ID
- 初期版では自動返信を送らない。受信確認後に必要性を判断する。
- メール本文やメールアドレスをアプリのログへ出さない。ログは受付ID、時刻、結果、種別だけにする。

`send.pygmix.com` を送信専用にすることで、`pygmix.com` の既存受信設定を維持したまま送信ドメイン認証を追加できる。実装時はResendが指定するDKIM、SPF、Return-Path関連レコードを送信専用サブドメインへ追加し、ルートドメインの既存MXレコードを変更しない。

### 環境設定

公開環境のSites設定にだけ登録し、ソースコードや `.env` をGitへ含めない。

- `RESEND_API_KEY`: 秘密情報
- `CONTACT_TO_EMAIL`: `info@pygmix.com`
- `TURNSTILE_SECRET_KEY`: 秘密情報
- `TURNSTILE_SITE_KEY`: 公開識別子。サーバーコンポーネントからフォームへ渡す。

### 入力検証と迷惑送信対策

- ブラウザ側とサーバー側の両方で同じ文字数・必須条件を確認する。
- Cloudflare Turnstileを送信時にサーバー側のSiteverify APIで検証する。期限切れ時はウィジェットを再取得する。
- 画面に見えない `website` フィールドを置き、値が入った送信を拒否する。
- リクエスト本文の上限を16KB程度に制限する。
- HTMLはプレーンテキストとして扱い、メール本文へ安全に整形する。
- APIキー、メールサービスの詳細エラー、内部情報をレスポンスへ返さない。

厳密な回数制限には永続ストアが必要になるため、初期版はTurnstile、ハニーポット、本文上限、送信サービス側の制限を使う。迷惑送信が実際に発生した場合だけ、Cloudflare側のレート制限またはKVを追加する。

### 利用者への表示

- 送信前: 「通常2〜3営業日以内に返信します。」
- 送信成功: 「お問い合わせを受け付けました。内容を確認して返信します。」
- 入力不備: 該当項目の直下へ日本語で表示し、先頭のエラー項目へフォーカスする。
- 一時的な失敗: 「送信できませんでした。時間をおいてもう一度お試しください。」
- 個別の受信アドレスは画面上へ直接掲載せず、フォームを主導線にする。

## 個人情報の扱い

`/privacy` に短いプライバシー案内を用意し、フォームからリンクする。

- 取得項目と利用目的を明記する。
- 返信と問い合わせ対応以外に利用しない。
- メール送信サービスを処理委託先として利用することを明記する。
- 問い合わせメールは対応完了から1年を目安に削除する運用を推奨する。
- データベースやブラウザのローカルストレージには保存しない。

## 実装時の順序

1. `info@pygmix.com` へ実際にテストメールを送り、転送先で受信できることを確認する。
2. Resendで `send.pygmix.com` を登録し、指定されたDNSレコード案を確認する。
3. 承認後にDNSレコードを追加し、送信ドメイン認証を完了する。
4. `/contact`、`/api/contact`、`/privacy`、Turnstileを実装する。
5. Sitesへ秘密情報を登録して公開候補を作る。
6. テスト問い合わせを1件送信し、`info@pygmix.com` での受信と返信先を確認する。
7. 受信確認後にのみ「フォーム送信が動作する」と判断し、公開版へ反映する。

## 完了条件

- 必須項目と文字数制限がブラウザ・サーバーの双方で機能する。
- 正常送信が `info@pygmix.com` に届く。
- 受信メールの返信操作で、入力者のメールアドレスが宛先になる。
- 二重送信、ボット送信、過大な本文、HTML入力を安全に扱える。
- 問い合わせ本文・メールアドレス・秘密情報がGitやログに残らない。
- モバイル、キーボード操作、スクリーンリーダーで入力とエラー確認ができる。
- 公開ページの見た目と文体が現在のPYGMIXサイトに合っている。

## 公式仕様の確認先

- Resend: <https://resend.com/docs/dashboard/domains/introduction>
- Cloudflare Turnstile: <https://developers.cloudflare.com/turnstile/get-started/server-side-validation/>
