# pygmix.com デザイントークンシート — ヒーロー見直し＋PICKUP
最終更新 2026-09-06／対象: `app/OrbitHero.tsx`（識別の帯）・`app/page.tsx` の `.pickup`（新設）

このシートが正。実装で値を変えたくなったら、コードより先にここを直す。
前版（円の中／円の外・上に埋め込んだPICKUP）は本人指示「もう少し大きくしたら大体の
イメージはOK。そろそろそもそものデザインの見直しも必要かもしれません」により**廃止**。
このシートは新しい構成（識別の帯を縦に詰め、PICKUPを独立セクション化）のみを記録する。

## 0. 設計メモ（design-policy.md の5つの問い）

1. **誰が・どの場面で**: pygmix.com のトップに来た人が、最初の数秒で「いま何を見てほしいか」を受け取る場面。
2. **どんな気分に**: 識別（PYGMIXは何者か）を短く受け取ったあと、「いま押しているのはこれ」を実物大に近い写真で見て、押したくなること。
3. **固有の核**: ヒーローは**回転寿司の比喩**（皿・コンベア・入口/出口の門）でできている。これは識別の帯の核であり続ける。**ただし「いま推す1つ」は、この比喩の中に押し込める必要はない** —（参照: 東京芸術劇場・Panicとも、トップ上段の押しは独立した帯を持っていた。docs/2026-09-06_hero-redesign-brief）。
4. **踏襲するもの / 意図的に外すもの**
   - 踏襲: 回転寿司の比喩（皿・コンベア・門）はそのまま。配色・書体・極小モノスペース（6〜8px・字間0.13〜0.18em）・「対角だけ極端な角丸」（`10px 64px 10px 10px` 等）。
   - 外す: PICKUPを流れる皿やコンベアの内側に埋め込むこと。円のサイズを膨らませて専用の場所を作ること（前版はこれで424pxまで広げていたが、640×336には届かなかった）。
5. **守る機能条件**: タップ44px以上／コントラスト4.5:1以上／`prefers-reduced-motion` で動きを止める（既存のグローバル指定で担保）／画像は同梱済みの640×336を使い、追加の読み込みを増やさない／W3C ARIA APG カルーセル要件（停止ボタン・ホバー/フォーカスで停止・`aria-live`）。

## 1. なぜ見直したか（実測・2026-09-06）

「舞台スケッチをこの倍くらいまで大きくしたい」＝約640×336px。1440×900 の旧ヒーロー
（100vh・円390px）にこの大きさが入る場所は無かった（円の上＝画面外、円の下・左右＝流れる
皿と208回衝突・円と重なる、を実測済み）。置き場所の調整では解けず、構成を変える必要があった。

## 2. 新しい構成

| 帯 | 内容 | 高さ（1440×900） |
|---|---|---|
| ① 識別（`.orbit-hero`） | 円・コンベア・門。比率のまま縮小 | 100vh→**560px**（`max(560px, 62svh)`） |
| ② PICKUP（`.pickup`、新設） | 640×336 相当の写真＋機能的な一行＋行き先。通常の文書の流れ（衝突を考えなくてよい） | 内容に応じて可変（カード実測 1180×384） |
| ③ ライブラリ（`.projects`） | 既存のまま | 既存のまま |

## 3. 識別の帯（`.orbit-hero`）の再調整値

円・コンベアは比率を保ったまま、新しい高さに収まるよう実測で縮めた。

| 変数 | 旧値（100vh想定） | 新値 | 根拠 |
|---|---|---|---|
| `min-height`（base, >900px） | `max(780px, 100svh)` | `max(560px, 62svh)` | 1440×900で560px |
| `--route-y` | `clamp(235px, 28vh, 275px)` | `clamp(120px, 15vh, 150px)` | 新ヒーロー高560pxに、コンベアが門・隅ラベル・ヒント文と重ならず収まる値を実測 |
| `--route-y-soft` | `clamp(155px, 19vh, 185px)` | `clamp(80px, 10vh, 100px)` | route-y と同じ縮小比（15/28≒0.536）を適用 |
| `--route-x` | `clamp(340px, 34vw, 480px)` | `clamp(280px, 30vw, 400px)` | 縦を大きく詰めたので横も穏やかに詰め、扁平になりすぎるのを防ぐ |
| `--route-x-soft` / `--route-x-inner` | 同上比率 | `clamp(230,23vw,325)` / `clamp(125,12.5vw,180)` | route-x と同じ縮小比（約0.833）を適用 |
| `min-height`（≤900px, 固定値だった） | `780px` | `500px` | 同じ縮小比（560/900）を適用 |
| `min-height`（≤600px, 固定値だった） | `820px` | `480px` | 同上 |

**実測（1440×900・1周52秒を通して）**: カードの最大下端489px（ヒーロー560pxに対し71px の余白。はみ出しなし）。768px・390pxでも同様に余白あり（それぞれ51px・60px）。

## 4. PICKUP（`.pickup`）— 新設セクション

`.orbit-hero` の直後、`.projects` の直前。通常の文書の流れなので、コンベアとの衝突判定は不要。

### 見出し（`.pickup-heading`）— `.projects-heading` と同型（kicker + h2）

| 項目 | 値 | 根拠 |
|---|---|---|
| kicker `PICKUP` | mono 8px・字間0.18em・`rgb(32 37 34 / 0.68)` | `.projects-heading > p:first-child` と同型。★0.62だと `var(--paper)` 上で4.26:1（基準4.5:1）に届かず、design-lint C1 実測で発見・0.68で5.11:1に修正 |
| `<h2>` | `clamp(26px, 2.8vw, 40px)` | `.projects-heading h2`（clamp(39,5.25vw,79)）より一段小さく。ライブラリ全体の見出しより主張しすぎない |

### カード（`.pickup-card`）— `.project-card` と同型の単一リンク

| 項目 | 値 | 根拠 |
|---|---|---|
| 内部レイアウト | `grid-template-columns: minmax(0,1.62fr) minmax(240px,1fr)` | 画像を主役に、コピーを右へ。760px以下で1カラムへ |
| 角丸 | `10px 64px 10px 10px` | 識別の帯の「対角だけ極端」を踏襲（四隅同径にしない） |
| 罫・ホバー | 1px `rgb(32 37 34/0.32)` → ホバーで `0.55`、`translateY(-3px)` | `.project-card`／`.orbit-card` と同型 |
| 画像 | `object-fit: cover`、`width=640 height=336`（実寸） | 640×336のOGカードをそのまま使う。追加の縮小コピーは作らない |
| カテゴリ行 | mono 8px・字間0.13em・`rgb(32 37 34 / 0.68)` | ★0.6だと `var(--paper-light)` 上で4.12:1に届かず、design-lint C1 実測で発見・0.68で5.27:1に修正 |
| 本文 | 13px / line-height 1.85 | `.project-card p`（11px/1.8）よりわずかに大きく、独立セクションとしての重みを付ける |
| アクション | mono 9px・`border-top: currentColor` | `.project-action` と同型（色は inherit のまま、`a{color:inherit}`） |

### 狭い幅（≤760px）

`.pickup-card { grid-template-columns: 1fr; }`。画像は `aspect-ratio: 640/336` で比率固定、`min-height` は解除。

## 5. あわせて直した不具合

- **`WORKS` の飛び先 `#activities` が存在しなかった**（押しても何も起きない）。`WORKS`→`#pickup`、`PROJECTS`→`#projects` と、別々の行き先に分けた。
- **自動回転するコンベアに `aria-live` が無かった**（W3C ARIA APG カルーセルの要件）。回転中は `off`、コンベアを止めると `polite` にした。`aria-atomic="false"` も付けた。
- **皿を11枚にした際、`aria-label` の人数表記が「10」のまま**だった（`入口から出口へ流れるPYGMIXの10のプロジェクト`）。「11」に直した。

## 6. 検証結果（2026-09-06）

- `npx tsc --noEmit` 通過／`npm test`（ビルド込み）7件全通過。
- design-lint: 変更直後は新規NG4件（コントラスト2件・カードの実装ミスではなく数値調整で解消）。修正後は **NG113**（旧・皿11枚の状態と同数＝新規の指摘ゼロ）。
- **WARN**: 旧14件→新18件（+4）。原因を実機で確認: 円が小さくなり門（入口/出口）とコンベアの距離が縮まったため、フレーム撮影時に皿が門の背後を通過する頻度が上がった。門の札は独自の不透明に近い背景（96〜97%）を持ち、視覚的には常に明瞭に読める（実機で確認）。design-lintは背後の合成色を判定できないだけで、実害ではない。
- 640×336のカードが、1440×900では折り返し無しで全部見える。768×1024・390×844でも、ヒーロー直後にPICKUPの全体（写真＋本文＋行き先）が1画面に収まることを実測。

## 7. 残っている検討事項（今回は着手していない）

- ライブラリ（`.projects`）の先頭に既存の「舞台スケッチ」抽象カード（P-05・全幅）が残っている。PICKUPと近接して同じ作品が2回出る形になっている。並べ替え・統合は今回のスコープ外（本人確認のうえ着手する）。
- 門の札のWARN増加（§6）を解消したい場合は、design-lint SPEC の推奨どおり「文字色を変えるより暗い帯（座布団）を敷いて背景を固定する」方式が確実（`.orbit-gate-label` の背景を完全不透明にする等）。

## 8. 追記 2026-09-06（つめる・PICKUPの文字拡大・タップ面の是正）

本人指示「回転寿司が広すぎるのでつめて舞台スケッチをよせましょう」「pickupの文字も大きく」。

### 8.1 コンベアの見た目のクリッピングを直した（根本原因）

実測（1440〜2560px幅、opacity 0.9以上の“ほぼ見えている”状態）で、皿が左右に
142〜266pxクリップされていた。本人の指摘スクショ（JuggleLineが「uggleLine」に
見切れていたもの）と一致。

原因は `@keyframes conveyor-route` の設計にあった。不透明度と位置が別々のタイミングで
切り替わっていた：
- 入場: 0%で位置=画面外・不透明度0 → **3%で不透明度だけ先に1** → 15%でやっと
  位置が画面内（-route-x）に到達。3%〜15%の間、**不透明なのに画面外**という区間があった。
- 退場: 84%で位置=画面内（route-x）→ **97%まで不透明度は1のまま** → 100%で位置が
  画面外（route-out）へ。同じ問題が逆向きに存在。

直し方: 3%・97%の単独キーフレームを削除し、不透明度を15%・84%（＝位置が画面内に
収まる地点）へ統合。同じ区間で不透明度と位置を一緒に内挿させることで、
「不透明なのに画面外」の瞬間が原理的に無くなった。
検証: 1440×900でopacity 0.5以上のクリップが0px（旧: 142px）。390×844では
steady-state（opacity 0.95以上）で最大7px（誤差レベル）。

### 8.2 横方向をつめた（実測値）

| 変数（base, >900px） | 前 | 後 |
|---|---|---|
| `--route-x` | `clamp(280px, 30vw, 400px)` | `clamp(240px, 25vw, 340px)` |
| `--route-x-soft` | `clamp(230px, 23vw, 325px)` | `clamp(195px, 19.5vw, 275px)` |
| `--route-x-inner` | `clamp(125px, 12.5vw, 180px)` | `clamp(105px, 10.5vw, 150px)` |
| `--route-out` | `calc(50vw + 160px)` | `calc(50vw + 80px)`（①でopacity内挿と連動したため、画面外へ大きく逃がす必要が減った） |

900px以下・600px以下のブレークポイントも同じ比率（約15%）でつめた。

### 8.3 PICKUPの文字を拡大した

| 項目 | 前 | 後 |
|---|---|---|
| `.pickup-heading > p`（PICKUP kicker） | 8px | **12px** |
| `.pickup-heading h2`（舞台スケッチ） | `clamp(26px, 2.8vw, 40px)` | **`clamp(34px, 3.8vw, 54px)`** |
| `.pickup-category`（STAGE / IMAGE STUDY） | 8px | 10px |
| `.pickup-note`（本文） | 13px | 15px |
| `.pickup-action`（体験版を使ってみる） | 9px | 11px |

### 8.4 ついでに見つけたタップ面の不足を直した（今回の見直しとは無関係の既存の不具合）

design-lintで新たに4件発見（`.center-menu nav a` 23px／`.orbit-toggle` 18px／
`.site-footer-wordmark` 28px／`.site-footer nav a` 13px、いずれも基準44px）。
今回の変更（route-x・PICKUP文字サイズ）はどれも触れていない要素だったので、
以前から存在していたが計測されていなかったと考えられる。いずれも
`min-height:44px` を足す形で44pxちょうどに是正（見た目の高さ・レイアウトは維持）。

### 8.5 検証

- design-lint: NG 113→**97**（皿11枚+PICKUP時のNGに、上記タップ面4件の修正分-14+2件のT1解消分-2を反映）。
  U1（タップ対象）は0件に。WARN は18→30。原因は実機確認: つめたことで皿どうし・
  皿と門の重なりが増え、design-lintが「背後の合成色を判定できない」と申告する頻度が
  上がったため（§6で確認した「門の札は独自の不透明背景を持ち視覚的には常に明瞭」と
  同じ性質。皿の説明文キャプションの行送り1.20も既存・全11皿共通の指摘で今回は触れていない）。
- npm test（ビルド込み）7件全通過。npx tsc --noEmit エラーなし。
